# Agent core
anthropic>=0.40.0

# Storage
psycopg2-binary>=2.9.9
numpy>=1.24.0

# Credentials
keyring>=24.0.0

# Google integrations (Tier 1)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0

# Scheduling + HTTP + config
APScheduler>=3.10.4
httpx>=0.27.0
python-dotenv>=1.0.0

# CLI / onboarding UI
rich>=13.7.0

# Voice I/O. The macOS/Windows wheels bundle PortAudio, so no system package
# is needed; Linux needs `apt install libportaudio2`.
sounddevice>=0.5.0
